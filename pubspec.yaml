name: fosszen_weather
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_auth: ^4.16.0
  http: ^1.2.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.2.0
  fluttertoast: ^8.2.4
  firebase_core: ^2.24.2
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
